/*Code to create the database. I created the database manually in mySQL.*/

/**
CREATE DATABASE CONTACT FORM

CREATE Table Contact(
  idContact   int(11)       NOT NULL    AUTO_INCREMENT,
  FirstName   varchar(20),
  LastName    varchar(20),
  PhoneNumber int(11),
  Email       varchar(30),
  Comments    varchar(500),
  CONSTRAINT idContact_PK PRIMARY KEY(idContact)
);
*/
